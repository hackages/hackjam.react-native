import React, {Component} from "react";
import io from "socket.io-client";
import {BACKEND_URL} from "../constants/backend";
import {GET_ALL_TRUCKS} from "../actions/trucks";

/**
 * TODO: Use this component to fetch the trucks from the backend's websocket
 * and dispatch them to Redux
 * http://calapez.me:4242
 */

export class TruckSocket extends Component {
  render() {
    return null;
  }
}
